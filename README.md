# Auth-Role-Based-Access-System
A backend API built with Node.js and Express.js for user authentication and role-based access control. Supports registration, login, protected routes, and admin-only APIs using middleware for secure access.
